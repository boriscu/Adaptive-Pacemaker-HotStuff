# Adaptive-Pacemaker-HotStuff
HotStuff is a Python simulator and visualizer for the HotStuff BFT protocol, implementing both Basic and Chained modes with a paper-faithful safety core. It adds a baseline and adaptive pacemaker (timeout tuning) and reports metrics like latency, throughput, and view changes.
